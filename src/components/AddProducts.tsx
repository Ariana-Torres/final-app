import React from 'react';

const AddProducts: React.FC = () => {
  return (
    <div>
      <h1>Agregar Productos</h1>
      {/* Agrega el formulario y la lógica para agregar productos */}
    </div>
  );
};

export default AddProducts;
